{
  "name": "stackcode",
  "version": "1.1.0",
  "private": true,
  "description": "An intelligent tool to help junior developers apply best practices from day one.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "npm run clean --workspaces --if-present",
    "build": "tsc --build && cpx \"packages/i18n/src/locales/**/*.json\" \"packages/i18n/dist/locales\" && cpx \"packages/core/src/templates/**/*\" \"packages/core/dist/templates\" && chmod +x packages/cli/dist/index.js"
  },
  "author": "Yago Borba",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@stackcode/cli": "^1.0.0",
    "@stackcode/core": "^1.0.0",
    "@types/node": "^24.1.0",
    "@types/yargs": "^17.0.33",
    "chalk": "^5.4.1",
    "cli": "^1.0.0",
    "cpx": "^1.5.0",
    "husky": "^9.1.7",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "inquirer": "^12.8.2",
    "yargs": "^18.0.0"
  }
}
