import { ExampleController } from './controllers/example.controller.js';
import { Logger } from './utils/logger.js';

/**
 * {{projectName}} - {{description}}
 * 
 * A Node.js + TypeScript application generated by StackCode.
 */

function main() {
  Logger.info('Starting {{projectName}}...');
  
  // Example usage of controller
  const welcome = ExampleController.getWelcome();
  const health = ExampleController.getHealth();
  
  Logger.info('Application initialized', { welcome, health });
  
  // Your application logic goes here
  Logger.info('{{projectName}} is running successfully!');
}

// Run the application
main();